const tones = ["Friendly", "Professional", "Casual", "Formal"];
export default function ToneSelector({ value, onChange }: { value: string; onChange: (value: string) => void }) { return <div className="flex flex-wrap gap-2">{tones.map(tone => <label key={tone} className={`cursor-pointer rounded-sm border px-4 py-2 text-xs ${value === tone.toLowerCase() ? "border-[var(--app-olive)] bg-[var(--app-olive)] text-white" : "border-[var(--app-input-border)] bg-[var(--app-surface)] text-[var(--app-ink)]"}`}><input className="sr-only" type="radio" name="tone" value={tone.toLowerCase()} checked={value === tone.toLowerCase()} onChange={e => onChange(e.target.value)} />{tone}</label>)}</div>; }
