const tones = ["Friendly", "Professional", "Casual", "Formal"];
export default function ToneSelector({ value, onChange }: { value: string; onChange: (value: string) => void }) { return <div className="flex flex-wrap gap-2">{tones.map(tone => <label key={tone} className={`cursor-pointer rounded-sm border px-4 py-2 text-xs ${value === tone.toLowerCase() ? "border-[#5a7a5a] bg-[#5a7a5a] text-white" : "border-[#e0ddd6] bg-white text-[#1a1a1a]"}`}><input className="sr-only" type="radio" name="tone" value={tone.toLowerCase()} checked={value === tone.toLowerCase()} onChange={e => onChange(e.target.value)} />{tone}</label>)}</div>; }
