const tones = ['Friendly', 'Professional', 'Casual', 'Formal']
export default function ToneSelector({ value, onChange }: { value: string; onChange: (value: string) => void }) { return <div className="flex gap-2">{tones.map(tone => <label key={tone} className={`cursor-pointer rounded-none border px-3 py-2 text-sm ${value === tone.toLowerCase() ? 'border-green bg-green/5 text-green' : 'border-[rgba(255,255,255,0.1)] text-[rgba(255,255,255,0.4)]'}`}><input className="sr-only" type="radio" name="tone" value={tone.toLowerCase()} checked={value === tone.toLowerCase()} onChange={e => onChange(e.target.value)} />{tone}</label>)}</div> }
